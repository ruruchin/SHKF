name: Release SHKF

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  windows:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install
        run: npm ci --ignore-scripts

      - name: Rebuild native modules for Electron
        run: npx electron-builder install-app-deps

      - name: Bundle org secrets for all users
        run: node scripts/write-org-secrets.mjs
        env:
          KONSTANCIA_YANDEX_API_KEY: ${{ secrets.KONSTANCIA_YANDEX_API_KEY }}
          KONSTANCIA_YANDEX_FOLDER_ID: ${{ secrets.KONSTANCIA_YANDEX_FOLDER_ID }}
          KONSTANCIA_CLOUD_URL: ${{ secrets.KONSTANCIA_CLOUD_URL }}
          KONSTANCIA_CLOUD_API_KEY: ${{ secrets.KONSTANCIA_CLOUD_API_KEY }}
          CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}
          MOBBIN_API_KEY: ${{ secrets.MOBBIN_API_KEY }}
          NANOBANANA_API_KEY: ${{ secrets.NANOBANANA_API_KEY }}

      - name: Cache electron-builder binaries
        uses: actions/cache@v4
        with:
          path: |
            ${{ env.LOCALAPPDATA }}\electron\Cache
            ${{ env.LOCALAPPDATA }}\electron-builder\Cache
          key: ${{ runner.os }}-electron-builder-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-electron-builder-

      - name: Build and publish
        uses: nick-fields/retry@v3
        with:
          timeout_minutes: 45
          max_attempts: 4
          retry_wait_seconds: 60
          command: npx electron-builder --win nsis --publish always
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CSC_IDENTITY_AUTO_DISCOVERY: 'false'

  linux:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install
        run: npm ci --ignore-scripts

      - name: Rebuild native modules for Electron
        run: npx electron-builder install-app-deps

      - name: Install Linux packaging tools
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y libarchive-tools rpm fakeroot dpkg \
            libgtk-3-0t64 libasound2t64 libatspi2.0-0t64 libnotify4t64 libxss1t64 libxtst6t64

      - name: Bundle org secrets for all users
        run: node scripts/write-org-secrets.mjs
        env:
          KONSTANCIA_YANDEX_API_KEY: ${{ secrets.KONSTANCIA_YANDEX_API_KEY }}
          KONSTANCIA_YANDEX_FOLDER_ID: ${{ secrets.KONSTANCIA_YANDEX_FOLDER_ID }}
          KONSTANCIA_CLOUD_URL: ${{ secrets.KONSTANCIA_CLOUD_URL }}
          KONSTANCIA_CLOUD_API_KEY: ${{ secrets.KONSTANCIA_CLOUD_API_KEY }}
          CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}
          MOBBIN_API_KEY: ${{ secrets.MOBBIN_API_KEY }}
          NANOBANANA_API_KEY: ${{ secrets.NANOBANANA_API_KEY }}

      - name: Cache electron-builder binaries
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/electron
            ~/.cache/electron-builder
          key: ${{ runner.os }}-electron-builder-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-electron-builder-

      - name: Build and publish Linux installers
        uses: nick-fields/retry@v3
        with:
          timeout_minutes: 45
          max_attempts: 3
          retry_wait_seconds: 60
          command: npx electron-builder --linux AppImage deb --publish always
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
